if [ "$TERM_PROGRAM" = "vscode" ]; then
  # Can't use the 1Password CLI from within VSCode on my system
  use flake
elif [ -z "$IN_NIX_SHELL" ]; then
  op run --env-file="./.env" -- nix develop --command "$SHELL"
fi